<% if object.errors.any? %>
  </br>
  <div class="row">
    <div id="error_explanation" class="col-12">
      <h4 class="text-muted"><%= pluralize(object.errors.count, "error") %> prohibited this <%= object.class.name %> from being saved:</h4>

      <ul class="list-unstyled">
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  </div>
<% end %>