<p id="notice"><%= notice %></p>

<!-- <h1>Tenant Fees</h1>

<table>
  <thead>
    <tr>
      <th>Tenant</th>
      <th>Amount</th>
      <th>Amount</th>
      <th>Recurring</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tenant_fees.each do |tenant_fee| %>
      <tr>
        <td><%= tenant_fee.tenant %></td>
        <td><%= tenant_fee.amount %></td>
        <td><%= tenant_fee.amount %></td>
        <td><%= tenant_fee.recurring %></td>
        <td><%= link_to 'Show', tenant_fee %></td>
        <td><%= link_to 'Edit', edit_tenant_fee_path(tenant_fee) %></td>
        <td><%= link_to 'Destroy', tenant_fee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br> -->




 <%= render "user_list", tenant_fees: current_account.tenant_fees, account: current_account %>

 <br>

 <%= link_to 'New Tenant Fee', new_tenant_fee_path %>
